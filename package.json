{
  "name": "skyward-compliance-agent",
  "version": "0.1.0",
  "description": "Compliance Q&A agent built on Cloudflare Workers and Workers AI",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"{src,schemas,docs}/**/*.{ts,js,md,json}\""
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "@types/node": "^20.14.12",
    "prettier": "^3.3.2",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.1"
  }
}
